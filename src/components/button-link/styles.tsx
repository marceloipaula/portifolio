import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    buttons: {
        fontSize: 30,
        color: 'white',
        backgroundColor: 'gray',
        padding: 10,
        borderRadius: 20,
        textAlign: 'center',
        margin: 5
    }
});
